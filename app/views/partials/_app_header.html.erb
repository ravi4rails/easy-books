<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">EASYBOOKS</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto ml-4">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Login<i class="fas fa-angle-down ml-2 "></i>
        </a>
        <div class="dropdown-menu rounded-0 mt-1" aria-labelledby="navbarDropdown">
          <% if user_signed_in? %>
            <div class= "text-center">
              <%= image_tag current_user.profile_pic_url, style: "width:70px;height:70px;border-radius:50%;"%>
            </div>
            <%= link_to "", class: 'dropdown-item' do %>
              <i class="far fa-user"></i> Hi <%= current_user.first_name %> 
            <% end %>
            <%= link_to edit_user_registration_path, class: 'dropdown-item' do %>
              <i class="far fa-edit"></i> Edit Profile
            <% end %>
            <%= link_to destroy_user_session_path, method: :delete, class: 'dropdown-item' do %>
              <i class="fas fa-sign-out-alt"></i> Logout
            <% end %>
          <% else %>
            <%= link_to new_user_session_path, class: 'dropdown-item' do %>
              <i class="fas fa-sign-in-alt"></i> Sign In
            <% end %>
          <% end %>
        </div>
      </li>
    </ul>
    <%= form_for :book_search, url: :book_search, method: :get, html: { class: "form-inline my-2 my-lg-0" } do |f| %>
      <%= f.text_field :keyword, class: "form-control mr-sm-2", placeholder: "Search", id: "tags" %>
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    <% end %>
  </div>
</nav>
